import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    width: 48,
    height: 48,
    borderRadius: 100,
    alignItems: 'center',
    justifyContent: 'center',
    marginVertical: 24,
    backgroundColor: '#9EAFDE',
  },
  icon: {
    color: '#282C40',
    marginLeft: -5,
  },
});
